name = "myfather-api"
main = "index.js"
compatibility_date = "2026-04-06"

[triggers]
crons = ["0 12 * * *"]

[observability]
enabled = true

# Set SUPABASE_URL, SUPABASE_SERVICE_KEY, DEEPSEEK_KEY,
# MP_ACCESS_TOKEN, MP_WEBHOOK_SECRET through encrypted Worker secrets.
# ADMIN_ORIGIN is the exact private dashboard origin (never '*').
